# ── Phase 1 ───────────────────────────────────────────────────────────────────

# Core RAG & LangChain
langchain==0.2.16
langchain-community==0.2.16
langchain-huggingface==0.0.3
langchain-chroma==0.1.4

# Vector Store
chromadb==0.5.7

# Embeddings (free, local — ~90MB download, then fully offline)
sentence-transformers==3.0.1

# Hybrid Search
rank_bm25==0.2.2

# Document Loaders
pymupdf==1.24.10        # PDF ingestion
beautifulsoup4==4.12.3  # HTML ingestion
openpyxl==3.1.5         # Excel support

# Structured Data
pandas==2.2.2
duckdb==1.0.0           # Replaces Snowflake for SQL queries
sqlalchemy==2.0.31

# Metadata & Tracking
mlflow==2.15.1
tqdm==4.66.5
python-dotenv==1.0.1

# Testing
pytest==8.3.2

# Utilities
rich==13.7.1             # Pretty terminal output
pyyaml==6.0.2
setuptools>=68.0.0       # Required for pkg_resources (mlflow dependency)
pydantic-settings>=2.0.0  # Required for chromadb 0.5.7 compatibility with pydantic v2
torch==2.2.0             # CPU version for Windows stability

# ── Phase 2 ───────────────────────────────────────────────────────────────────

# Local LLM via Ollama (install Ollama separately from https://ollama.com)
langchain-ollama==0.1.3
# cross-encoder reranker is included in sentence-transformers above

# ── Phase 3 ───────────────────────────────────────────────────────────────────

# Multi-agent orchestration with LangGraph
langgraph==0.2.28
typing-extensions==4.12.2   # Required by LangGraph TypedDict at runtime

# ── Phase 4 (install now so imports don't break) ──────────────────────────────

# Evaluation
ragas==0.1.21

# Serving
fastapi==0.115.0
uvicorn==0.30.6
streamlit==1.38.0