# Core RAG & LangChain
langchain==0.2.16
langchain-community==0.2.16
langchain-huggingface==0.0.3
langchain-chroma==0.1.4

# Vector Store
chromadb==0.5.7
pydantic-settings>=2.0.0  # Required for chromadb 0.5.7 compatibility with pydantic v2

# Embeddings (free, local)
sentence-transformers==3.0.1

# Hybrid Search
rank_bm25==0.2.2

# Document Loaders
pymupdf==1.24.10        # PDF ingestion
beautifulsoup4==4.12.3  # HTML ingestion
openpyxl==3.1.5         # Excel support

# Structured Data
pandas==2.2.2
duckdb==1.0.0           # Replaces Snowflake for SQL queries
sqlalchemy==2.0.31

# Metadata & Tracking
mlflow==2.15.1
tqdm==4.66.5
python-dotenv==1.0.1

# Evaluation (Phase 4, installed now so imports don't break)
ragas==0.1.21

# Testing
pytest==8.3.2

# Utilities
rich==13.7.1             # Pretty terminal output
pyyaml==6.0.2
setuptools>=68.0.0       # For pkg_resources functionality
torch==2.2.0             # CPU version for compatibility


# ── Phase 2 ──────────────────────────────────────────────────────────────────
# Local LLM via Ollama
langchain-ollama==0.1.3
# sentence-transformers already listed — cross-encoder is included